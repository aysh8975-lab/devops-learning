# Navigation

pwd
ls
ls -l
ls -a
cd 
cd ..
cd /tmp
cd /home/ubuntu

# File Navigation
touch example.txt
mkdir /projects/test_dir
cp example.txt /projects/test_dir
mv projects/test_dir/example.txt projects/test_dir/backup_example.txt

# File Viewing

vim example.txt
nano example.txt
head
tails
cat example.txt | head
tail /var/log/syslog

# My top 5 most useful commands
ls -la shows permissions and any hidden files or directiories 
grep searching inside file can pipe into other commands like strings
chmod changes file/directory permissions
less opens filess one page at a time so we can search through the file slowly using less power
sort does what it says example sort data.txt | uniq -u this prints out the unique line of text in the file onto the terminal

 
